```CPP
enum TileType
{
	TILE_TYPE_INVALID = -1.
	TILE_TYPE_GRASS,
	TILE_TYPE_STONE,
	NUM_TILE_TYPES
};

TileDef TileDef::g_tileDefs [NUM_TILE_TYPES];

class TileDef
{
	bool m_isSolid = false;
	AABB2 m_UVs  = AABB( Vec2(0.f, 0.f), Vec2(1.f, 1.f);
	Rgba8 m_tint = Rgba8(255, 255, 255);
	
public:
	static void InitializeTileDefs();
	
private:
	static const TileDef s_tileDefs[NUM_TILE_TYPES];
}

// TileDef::InitalizeTileDefs :: Game Constructor
s_tileDefs[TILE_TYPE_GRASS].m_isSolid = true;
s_tileDefs[TILE_TYPE_GRASS].m_tint = Rgba8( 255, 255, 255 );
s_tileDefs[TILE_TYPE_GRASS].m_UVs = g_terrainSpriteSheet.GetUVsForSpriteCoords(2,1);
	
for (int tileType = 0; tileType < NUM_TILE_TYPES; ++tileType )
{
	TileDef const& tileDef = s_tileDefs[i];
}

map->HasLineOfSight( PosA, PosB)
{
	

	RayCastResult2D result = RaycastVsTiles( posA, fwdNormal. maxDist );
	return !result.m_didImpact;
}


RaycastResult2d RaycastVsTiles

```