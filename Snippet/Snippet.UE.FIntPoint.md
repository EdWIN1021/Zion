# FIntPoint

Type: Struct

<aside>
💡

An `FIntPoint` is a structure in Unreal Engine used to represent a 2D coordinate using **two whole numbers (integers)**.

</aside>

```cpp
/* FIntPoint(Col, Row) */
const FIntPoint Dimension = FIntPoint(1, 1);
```