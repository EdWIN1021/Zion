# GetSessionIdStr

Access Specifier: public
Description: the session id for a given session search result
Tags: function

```cpp
	FString GetSessionIdStr() const
	{
		return Session.GetSessionIdStr();
	}
```

```cpp
FString Id = Result.GetSessionIdStr();
```