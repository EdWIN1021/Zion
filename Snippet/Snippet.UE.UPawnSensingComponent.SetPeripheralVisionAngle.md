# SetPeripheralVisionAngle

Category: Function

<aside>

The angle at which the pawn can detect pawns outside of its direct line of sight but still within the sight radius.

</aside>

```cpp
PawnSensing->SetPeripheralVisionAngle(45.f);
```