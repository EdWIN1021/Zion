# OnChaosBreakEvent

Tags: Event

<aside>

`OnChaosBreakEvent` is a multicast delegate on `UGeometryCollectionComponent` that fires whenever a fracture piece separates during a Chaos simulation. 

</aside>

General

- Notify Breaks → true