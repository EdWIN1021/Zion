# GetValueAtLevel

Category: Function

<aside>

Returns the scaled value at a given level

</aside>

```cpp
float GetValueAtLevel(float Level, const FString* ContextString = nullptr) const;
```

```cpp
GetHeroCurrentEquippedWeapon()->HeroWeaponData.WeaponBaseDamage.GetValueAtLevel(InLevel);
```