# TSet

<aside>
💡

A `TSet` is a container in Unreal Engine that stores a collection of **unique** elements in an **unordered** way.

</aside>

```cpp
TSet<int32> CheckedIndices;
```

## Contains

```cpp
CheckedIndices.Contains(Index);
```

## Append

```cpp
CheckedIndices.Append(TentativelyClaimed);
```