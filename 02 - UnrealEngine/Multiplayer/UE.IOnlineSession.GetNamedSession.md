# GetNamedSession

Text: Its purpose is to retrieve a pointer to a session object (FNamedOnlineSession) by its name.

```cpp
	auto ExistingSession = OnlineSessionInterface->GetNamedSession(NAME_GameSession);
```